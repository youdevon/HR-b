/**
 * Toggle Record Keeping (HR records module) in navigation, dashboards, and admin UIs.
 * When false, `/records` routes redirect and the module is hidden; database tables and
 * permission rows are unchanged. Set to true to restore the module.
 */
export const RECORD_KEEPING_UI_ENABLED = false;
